{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "phone_number": {
      "type": "string",
      "description": "User's phone number for ride contact"
    }
  },
  "required": [
    "role"
  ]
}